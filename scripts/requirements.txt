# Runtime dependencies
pyserial>=3.5
matplotlib>=3.3
# 6.5+ for QStyleHints.colorScheme() used by the auto light/dark theme
PyQt6>=6.5

# Build dependencies (for creating standalone executable)
# pyinstaller>=6.0

# Test dependencies (optional)
# pytest>=7.0
