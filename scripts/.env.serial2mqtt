# AirCube MQTT bridge — copy to the Pi and run from scripts/ (or set path in load_dotenv)

AIRCUBE_PORT=/dev/cu.usbmodem21101

MQTT_HOST=10.10.1.10
MQTT_PORT=1883
# MQTT_USER=
# MQTT_PASS=

AIRCUBE_NAME=AirCube
AIRCUBE_ID=aircube_1

HA_DISCOVERY_PREFIX=homeassistant
AIRCUBE_INTERVAL=5
